language: python
sudo: false

matrix:
  include:
    - python: 3.4
    - python: 3.5
    - python: 3.6
    - python: 3.7
      dist: xenial
      sudo: true


install:
  - python3 setup.py build install
  - pip install python-coveralls

script:
  python3 setup.py lint test

after_success:
  coveralls
